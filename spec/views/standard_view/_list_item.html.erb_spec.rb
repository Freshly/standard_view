# frozen_string_literal: true

require "rails_helper"

RSpec.describe "standard_view/_list_item.html.erb", type: :view do
  helper(StandardView::BadgeHelper)

  # subject(:render_partial) { render partial: "standard_view/list_item", locals: { material: material } }

  it "needs specs"
end
